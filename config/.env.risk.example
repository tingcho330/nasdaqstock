# (사용 안 함) 리스크 Discord 웹훅은 config/.env 에만 설정하세요.
#
#   DISCORD_WEBHOOK_URL=...        # integrated_manager 등
#   DISCORD_WEBHOOK_URL_RISK=...   # background_risk_manager 전용
#
# docker-compose 는 config/.env 만 로드합니다. 이 파일(.env.risk)은 만들 필요 없습니다.
